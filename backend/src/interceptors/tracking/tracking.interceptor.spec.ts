import { TrackingInterceptor } from "./tracking.interceptor";

describe("TrackingInterceptor", () => {
  it("should be defined", () => {
    expect(new TrackingInterceptor()).toBeDefined();
  });
});
