export const MAX_SKELETON_LOADER_ITEMS = 12;
export const MAX_SAVED_PIPELINES_AT_VIEW_TAB = 6;

export enum LoadingType {
  SKELETON = "SKELETON",
  SPINNER = "SPINNER",
}

export enum ViewType {
  LIST = "LIST",
  GRID = "GRID",
  TABLE = "TABLE",
}
